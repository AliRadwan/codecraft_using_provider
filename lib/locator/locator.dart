
import 'package:get_it/get_it.dart';

final locator = GetIt.instance;

void setUp(){

  // locator.registerLazySingleton<HttpClient>(() => HttpClient());
  // locator.registerLazySingleton<BaseLoginData>(() => LoginDataImpl(sl()));

}